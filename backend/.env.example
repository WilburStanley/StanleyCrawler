# Adjust these if your local ports differ from the defaults below.
SCRAPER_SERVICE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3001

# Must match scraper-service's own INTERNAL_API_KEY exactly.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
INTERNAL_API_KEY=